<!-- Scaffolded by the `project-structure` skill. This folder is pre-created empty on purpose:
     the convention names it, so every project has it in the same place under the same name and no
     session has to invent one. An empty folder here is not clutter to clean up. -->

# To-Implement Notes

Work identified but not yet scheduled: follow-ups, deferred cleanups, and "we
should eventually" items that have no story folder yet.

One file per theme, kebab-case. When an item becomes real work, move it into its
`00N-<ticket-id>-<name>/` story folder and delete it here — this folder is a
staging area, not a second backlog.

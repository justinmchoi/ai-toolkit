# Portable .NET/C# service repo shape - not tied to any employer. Mirrors
# es-ai-toolkit's dotnet-service preset using this toolkit's own general-purpose
# packs in place of any employer-specific ones (no OTel/tracker-hygiene packs here).
# Excludes sql-server-safety on purpose - not every service repo does direct
# SQL Server data-fix work; add it ad hoc (baseline apply sql-server-safety)
# when a session actually needs it.
karpathy-principles
git-collaboration-hygiene
repo-context-grounding
commit-conventions
commit-attribution
branch-naming
pr-description
code-doc-sync
code-review-reporting
oop-extension-safety
startup-config-validation
dotnet-conventions
powershell-conventions
process-vs-work-doctrine
layered-ownership
verification-epistemics
handoff-doc-discipline

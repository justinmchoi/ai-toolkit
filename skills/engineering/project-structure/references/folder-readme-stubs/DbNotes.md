<!-- Scaffolded by the `project-structure` skill. This folder is pre-created empty on purpose:
     the convention names it, so every project has it in the same place under the same name and no
     session has to invent one. An empty folder here is not clutter to clean up. -->

# Database Notes

Schema exploration, query recipes, table relationships, and data-shape findings
for this project's databases.

One file per database, schema, or investigation. Keep credentials and connection
strings out — reference where they live instead.

Distinct from `Glossary/`, which defines what a *business* term means;
this is where the data actually sits and how to query it.

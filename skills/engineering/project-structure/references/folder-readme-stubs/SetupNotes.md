<!-- Scaffolded by the `project-structure` skill. This folder is pre-created empty on purpose:
     the convention names it, so every project has it in the same place under the same name and no
     session has to invent one. An empty folder here is not clutter to clean up. -->

# Setup Notes

How to get set up to work on this project: local environment, tool versions,
access requests, credentials (where they live, never their values), and the
commands that verify the setup works.

Written for whoever picks this project up next, including yourself in three
months.
